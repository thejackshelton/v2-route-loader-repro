# Reproduction Steps:

1. Run `pnpm dev`
2. Open the browser and navigate to `http://localhost:5173`
3. Open the console and you will see the message `randomThing` logged, despite us never consuming the route loader anywhere in the app.
